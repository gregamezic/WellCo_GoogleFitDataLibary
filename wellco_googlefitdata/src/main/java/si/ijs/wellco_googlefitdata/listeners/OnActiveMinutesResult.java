package si.ijs.wellco_googlefitdata.listeners;

public interface OnActiveMinutesResult {

    /**
     * Interface callback with Active minutes duration
     * @param activeMinutes
     */
    void onActiveMinutesDataResult(long activeMinutes);
}
